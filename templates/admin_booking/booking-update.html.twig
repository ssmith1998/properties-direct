{% extends 'adminBase.html.twig' %}

{% block title %}Dashboard{% endblock %}

{% block body %}

<div class="wrapper" style="margin-top: 127px">

<div class="container">
{{ form_start(form) }}

    
        <div class="form-group">
            {{ form_row(form.property) }}
        </div>
        <div class="form-group">
            {{ form_row(form.dateOfViewing) }}
        </div>

         <div class="form-group">
            {{ form_row(form.timeOfViewing) }}
        </div>
         {# <div class="form-group">
            {{ form_row(form.property) }}
        </div> #}
         <div class="form-group text-center">
            {{ form_row(form.update) }}
        </div>
    
{{ form_end(form) }}
</div>
</div>

{% endblock %}